package com.neonpaw.app.ui.theme

import androidx.compose.ui.graphics.Color
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.unit.sp

val TerminalBlack = Color(0xFF000000)
val TerminalGreen = Color(0xFF00FF88)
val TerminalGreenDim = Color(0xFF00FF88).copy(alpha = 0.5f)
val TerminalGreenFaint = Color(0xFF00FF88).copy(alpha = 0.2f)
val TerminalRed = Color(0xFFFF4444)
val TerminalPanelBg = Color(0xFF004D26).copy(alpha = 0.15f)

val MonoFont = FontFamily.Monospace
val AsciiFontSize = 9.sp
val LabelFontSize = 10.sp
